import type {Language} from './types';

/** `KELYSTY`: List of all available languages by default */
export const DEFAULT_LANGUAGES: Language[] = [{name: 'english', code: 'en'}];

/** `KELYSTY`: Default language */
export const DEFAULT_LANGUAGE: Language = {
  name: 'english',
  code: 'en',
};
