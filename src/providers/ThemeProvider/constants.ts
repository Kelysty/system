import type {DesignedTheme, Theme} from './types';

/**
 * `KELYSTY`: List of all available themes by default
 */
export const DEFAULT_THEMES: Theme[] = ['system', 'light', 'dark', 'alternative'];

/**
 * `KELYSTY`: Default theme
 */
export const DEFAULT_THEME: Theme = 'system';

/**
 * `KELYSTY`: Default designed theme
 */
export const DEFAULT_DESIGNED_THEME: DesignedTheme = 'light';
